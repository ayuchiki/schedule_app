<h2>スケジュール詳細</h2>
 
 <table>
   <tr>
     <th>タイトル</th>
     <td><%= @list.title %></td>
   </tr>
   <tr>
     <th>開始日</th>
     <td><%= @list.start_day %></td>
   </tr>
   <tr>
     <th>終了日</th>
     <td><%= @list.end_day %></td>
   </tr>
   <tr>
     <th>終日</th>
     <td>
      <%  if @list.all_day == true  %>
            ○
      <%  else  %>

      <% end %>
     </td>
   </tr>
   <tr>
     <th>スケジュールメモ</th>
     <td><%= @list.memo %></td>
   </tr>
 </table>
 
 <div>
   <ul>
     <li><%= link_to "スケジュール一覧に戻る", :lists %></li>
     <li><%= link_to "スケジュール一を編集する", "/lists/#{@list.id}/edit"%></li>
     <li><%= link_to "スケジュール一を削除する", "/lists/#{@list.id}", method: :delete, data: { confirm: "本当に削除しますか？" }%></li>
   </ul>
 </div>

 <footer>
  <h1>現在の日時：　<%= Date.current.strftime("%Y年 %m月 %d日") %></h1>
</footer>

 
